{
  "name": "@workssh/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && XDG_CONFIG_HOME=/tmp/workssh-wrangler-config wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260723.1",
    "typescript": "7.0.2",
    "wrangler": "4.113.0"
  }
}
